# What is JSX, and why is it used?
JSX (JavaScript XML) is a syntax extension for JavaScript that looks similar to HTML.
# - What is the difference between State and Props?
Internal data managed by a component,,	External data passed into a component.
# What is the useState hook, and how does it work? 
useState is a React Hook that lets you add state to functional components.
# How can you share state between components in React?
Lift state up: Move the state to the nearest common parent and pass it down via props.
# - How is event handling done in React?
Events in React are handled using event handlers written in JSX.
